CompileFlags:
  CompilationDatabase: "build"
  Add: [-std=c++20]
  # Suggesting a query-driver to find system headers on Windows (clang/gcc)
  # This helps clangd find headers like <cstdint> if it can't find them automatically.
  QueryDriver: ["C:/Program Files/LLVM/bin/clang++.exe", "C:/msys64/mingw64/bin/g++.exe", "clang++", "g++"]

Index:
  StandardLibrary: Yes

Diagnostics:
  UnusedIncludes: Strict
  ClangTidy:
    Add: [performance-*, bugprone-*, readability-*, modernize-*]
    Remove: [modernize-use-trailing-return-type]
